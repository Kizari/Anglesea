<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anglesea">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.MainActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.CalculationActivity" />
        <activity android:name=".Activities.RoomListActivity" />
        <activity android:name=".Activities.AddDrugActivity" />
        <activity android:name=".Activities.DrugListActivity" />
        <activity android:name=".Activities.IVOralActivity" />
        <activity android:name=".Activities.NurseRegActivity" />
        <activity android:name=".Activities.PatientSummaryActivity" />
        <activity android:name=".Activities.CriticalDrugActivity"></activity>
    </application>

</manifest>