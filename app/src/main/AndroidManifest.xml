<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anglesea">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.MainActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.CalculationActivity" android:label="Drug Administration" />
        <activity android:name=".Activities.RoomListActivity" android:label="Hospital Overview" />
        <activity android:name=".Activities.AddDrugActivity" android:label="Drug" />
        <activity android:name=".Activities.DrugListActivity" android:label="Drug List" />
        <activity android:name=".Activities.IVOralActivity" android:label="Administration Type" />
        <activity android:name=".Activities.NurseRegActivity" android:label="Registration" />
        <activity android:name=".Activities.PatientSummaryActivity" android:label="Visit Summary" />
        <activity android:name=".NurseLoginActivity" android:label="Nurse Login" />
        <activity android:name=".PatientRegActivity" android:label="Patient" />
        <activity android:name=".Activities.PatientDetailActivity" android:label="Patient Detail"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>